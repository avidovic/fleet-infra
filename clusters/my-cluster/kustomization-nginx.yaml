apiVersion: source.toolkit.fluxcd.io/v1
kind: HelmRepository
metadata:
  annotations:
    source-file: kustomization-nginx.yaml
  name: source-nginx.yaml
  namespace: nginx
spec:
  interval: 5m0s
  url: https://charts.bitnami.com/bitnami
